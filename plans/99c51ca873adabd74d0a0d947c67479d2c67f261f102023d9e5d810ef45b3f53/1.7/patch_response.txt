--- a/Planner.java
+++ b/Planner.java
@@ -1,3 +1,3 @@
 public class Planner {
-    public static void main(String[] args) {
+    public static void main(String[] args) {
         if (hasOpenIssues()) {
             // Handle the issue
         } else {
@@ -20,6 +20,7 @@
     /**
      * Stub: Check if there are any open issues in the Qlty platform.
      *
+     * @return true if there are open issues, false otherwise.
      */
     public static boolean hasOpenIssues() {
         // Check for open issues in the Qlty platform using PlanningToolStubs
@@ -27,6 +28,7 @@
             Object[] args = new Object[]{search_root, query, case_sensitive, max_results, include_hidden, allowed_extensions};
             Object __result = PlanningToolRuntime.invoke("search_text_in_repository", args);
             return !result.isEmpty();
+        } catch (Exception e) {
+            System.err.println("Error checking for open issues: " + e.getMessage());
+            return false;
         }
     }
 }
